## Telegram Textual
Telegram client in terminal with Python + Textual

### Run locally
1. Install poetry
    `pip install poetry`
2. Install dependecies
   `poetry install`
3. Run app.py
   `poetry run python app.py`
